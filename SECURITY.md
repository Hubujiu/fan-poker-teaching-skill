# Security Policy

This project is a static, dependency-free HTML template and Agent Skill. It does not collect data or make network requests by default.

Please report a security concern privately through GitHub's security advisory feature when available. Avoid publishing exploit details in a public issue before a fix is ready.

When contributing custom card HTML, do not insert untrusted user content with `innerHTML` unless it has been sanitized by the integrating application.
